# =====================================================================
#  02. 센서 여러 개로 예측하기 + 정직하게 채점하기 (train / test)
#  실행: python 02_선형회귀_다변수_train_test.py   (수업코드 폴더에서)
# =====================================================================
# 01 예선 입력이 공기온도 '하나'였습니다. 이번엔 네 개(공기온도 회전수 토크 공구마모)를 한꺼번에 봅니다
# 바뀌는 건 딱 두가지에요.
#
# 입력 하나 -> 입력 여러 개 : 예측 = w1x공기온도 + w2x회전수 + w3x토크 + w4x공구마모 + b
# 채점을 '학습에서 안 쓴 데이터'로 한다 -> 진짜 실력이 나오고, 과적합이 보인다
# 걷는 방법(경사하강, 01)은 한 글자도 안 바뀝니다. 손잡이가 2개에서 5개로 늘어났을 뿐.


import os
import numpy as np
import pandas as pd

DATA = os.path.join(
    os.path.dirname(os.path.abspath(__file__)), "..", "수업용데이터"
)  # data/수업용데이터/
df = pd.read_csv(os.path.join(DATA, "11_설비센서_ai4i.csv"), encoding="utf-8-sig")


# =====================================================================
# 1. 입력이 여러 개면 X 는 '표' — 행 = 설비 한 대, 열 = 센서 하나
# =====================================================================
# 특징(feature) = 입력으로 쓰는 값 하나하나. 공기온도도 특징, 토크도 특징 . x 의 열 하나 = 특징
# - 특징이 여러개인 성형회귀를 '다변수 선형회귀'라고 부릅니다. 이름만 길지 하는 일은 같음.
#
# 왜 여러개를 쓰나? - 온도 하나보다 온도 + 회전수 + 마모를 같이 보면 더 잘 맞을 수 있고
#               - 어느 센서가 진짜 중요한가를 가중치 크기로 읽을 수 있음


특징이름 = ["공기온도", "회전수", "토크", "공구마모"]
X = df[특징이름].values.astype(float)
# df[리스트] = 그 열들만 뽑은 표 → .values = numpy 표(200행 × 4열)
y = df["공정온도"].values.astype(float)  # 정답은 여전히 숫자 200개 한 줄
print("[1] X 의 모양 (행, 열):", X.shape, " y 의 모양:", y.shape)
print(
    "    1번 설비의 센서값 4개:", X[0]
)  # X[0] = 0번 행 = 첫 설비의 센서값 4개 (00 미리보기 ⑦)
print("    1번 설비의 정답:", y[0])

# 읽는 법 : x 는 200명의 학생의 ' 국 영 수 과 ' 점수표 , y는 그 학생들의 '내신 등급'같은거
# 한 행이 한 학생(설비) , 한 열이 한 과목 ( 센서 )

# (np.abs 는 화면에 '-0'이 찍히는 걸 막으려고 붙였습니다. 값은 0 입니다. )


# =====================================================================
# 2. 학습용 / 시험용 나누기 — 시험 문제를 미리 보지 않기 !
# =====================================================================
# -01 에선 200대 전부 배우고 200대 전부로 채점했습니다. 이건 '답안지 보고 푼 시험'이라 점수를 믿을 수 없음
# 그래서 시작하자마자 30%를 떼어 금고에 넣고 , 마지막 채점에만 꺼냅니다. 학습중엔 절대 안 봄.
# train (학습용) = 배우는데 쓰는 데이터 (교과서)
# test (시험용) = 배운 적 없는 데이터로 실력을 재는 데 쓰는 데이터 (시험지)
# 일반화 = '안 본 데이터'에서도 맞히는 능력. 우리가 진짜 원하는 것. 시험용 점수가 그 척도
# - 왜냐 현장에 들어가는 순간부터 들어오는 데이터는 전부 ' 안 본 데이터' 니까

rng = np.random.RandomState(
    42
)  # 무작위 기계에 번호 (42)를 붙이면 , 몇 번 돌려도 같은 무작위가 나옴
순서 = rng.permutation(
    len(X)
)  # (인덱스 번호)0~199 를 무작위로 섞은 순서표. permutation = "섞어라"
n_train = int(
    len(X) * 0.7
)  # 200 × 0.7 = 140.0 → int() 로 소수점 버려 140 (30% 만 빼기 )
tr, te = (
    순서[:n_train],
    순서[n_train:],
)  # 섞은 순서의 앞 140개 번호 = 학습용, 뒤 60개 번호 = 시험용 (00 미리보기 ⑥ 슬라이스)
X_train, X_test = X[tr], X[te]  # 문법 - 그 번호의 행들만 골라 새 표를 만듦
y_train, y_test = (
    y[tr],
    y[te],
)  # 정답도 '같은 번호'로 골라야 짝이 안 어긋납니다. 이게 제일 중요!!!!!
print("\n[2] 학습용", len(tr), "대 / 시험용", len(te), "대")

# 01에서는 x가 200개 짜리 한 줄이였음
# (200 , 4)
# 설비당 답 하나 나옴.
#
# 200 문제 짜리 문제집이 있음 . 140 문제로 공부하고 ,
# 나머지 60 문제는 봉투에 넣어 봉인. 시험 볼 떄 만 뜯음
# 01 마지막에 봤습니다 배운걸 다시 물어보면 만점이 나옵니다.
# 그건 실력이 아니라 기억력입니다.
#
# 파일이 날짜순이나 설비순이나 정렬돼 있으면, 앞 140 은 1월것,
# 뒤 60은 12월 것 이런식으로 될 수 있다
#
# 왜 7: 3 ? 관습입니다. 8:2 도 많이 씁니다. 데이터가 적을수록 학습용을 더 챙깁니다.
#


# =====================================================================
# 3. 표준화 — 열마다 따로, 그리고 학습용의 평균·표준편차만 써서
# =====================================================================
# 네 센서의 숫자 크기가 제각각입니다 . 학습용 평균을 보면 :
#
print("\n[3] 열별 원래 크기 (학습용 평균):", X_train.mean(axis=0).round(1))
# axis =0 '세로방향으로 ' 열마다 하나씩 , 4 열이니 평균이 4개가 나옵니다(axis =1 이면 행마다 나옴) ( 특징 4개를 줘서)
# 온도 300 , 회전수 1500, 토크 40 , 마모 120 전부 다릅니다.

# 표준화 없이 그냥 걸으면 어떻게 되나 - 01 발산 이 훨씬 심해집니다 (손잡이가 4개라 튕길 방향도 4개).


def 손실_원래눈금(X, y, w, b):
    return np.mean((y - (X @ w + b)) ** 2)


print("    표준화 안 하고 20걸음 걸어 보기:")
for lr in [0.1, 0.0000001]:
    w0, b0 = np.zeros(4), 0.0
    for _ in range(20):
        gw = np.array(
            [
                (
                    손실_원래눈금(X_train, y_train, w0 + np.eye(4)[j] * 1e-4, b0)
                    - 손실_원래눈금(X_train, y_train, w0 - np.eye(4)[j] * 1e-4, b0)
                )
                / 2e-4
                for j in range(4)
            ]
        )
        gb = (
            손실_원래눈금(X_train, y_train, w0, b0 + 1e-4)
            - 손실_원래눈금(X_train, y_train, w0, b0 - 1e-4)
        ) / 2e-4
        w0, b0 = w0 - lr * gw, b0 - lr * gb
    print(
        f"      lr={lr:<10} → 손실 {손실_원래눈금(X_train, y_train, w0, b0):.2e}"
    )  # {:.2e} = 지수 표기 (1.18e+39 = 1.18 × 10³⁹)

# ls = 0.1 -> 손실이 10 의 39제곱. 발산(튕겨나감) . 회전수 1500 이 곱해져 한 걸음이 너무 커서
# ls = 0.0000001 -> 안 튕기긴 하는데 손실 1106. 20걸음으로는 어림도 없음(굼벵이).
# 온도쪽은 그 lr로 너무 작아서
# 즉 '회전수에 맞는 보폭'과 '온도에 맞는 보폭'이 다른데 학습률은 하나뿐 -> 어느 쪽에 맞춰도 망함
# 해결 ; 열마다 자를 똑같이 맞춘다 = 열별 표준화 . 그러면 lr 하나 (0.1 )로 다 됩니다.

mu = X_train.mean(axis=0)
sd = X_train.std(axis=0)  # 열별 표준편차 4개 (퍼진 정도) <- 학습용으로만 계산** 중요 **
Z_train = (
    X_train - mu
) / sd  # 열마다 (값 − 그 열 평균) ÷ 그 열 표준편차. numpy 가 열 맞춰 알아서 해 줍니다
Z_test = (
    (X_test - mu) / sd
)  # 열마다 ( 값 - 그 열 평균) 나누기 그 열 표준편차 . numpy가 열 맞춰 알아서 해 줍니다.
print(
    "    표준화 후 학습용 열별 평균:", np.abs(Z_train.mean(axis=0)).round(3), "← 전부 0"
)
print(
    "    표준화 후 학습용 열별 퍼짐:", Z_train.std(axis=0).round(3), "← 전부 1"
)  # *시험용도 '학습용의 mu ,st'로  변환 *
print(
    "    표준화 후 시험용 열별 평균:",
    Z_test.mean(axis=0).round(3),
    "← 0 이 아님. 학습용 자를 빌려 썼으니 당연",
)

# 왜 시험용은 시험용 평균으로 안 하나? 두가지 이유.
# 1. 시험용 정보가 학습에 새어 들어가면 (누수) 점수가 부풀려 집니다. ' 시험 문제 미리 보지 않기 ' 의 연장선
# 2. 현장에선 새 데이터가 한 줄씩 들어옵니다. 한 줄로 평균을 낼 수 없으니 . 어ㅉ빠피 학습 때 자를 계속 써야 함.


# =====================================================================
# 4. 예측과 학습 — 01 과 똑같고, w 가 숫자 하나에서 '숫자 4개'가 될 뿐
# =====================================================================
# 설비 한 대의 예측 = w1z1 + w2z2 + w3z3 + w4z4 + b ('센서값에 가중치 곱해서 다 더하기')
# 이걸 140대에 한 번에 쓰는 게 행렬곱 z@w입니다
# @ = 행렬곱 . " 각 행에 대해 , 곱해서 더하기"를 한꺼번에 (140행 * 4열 ) @ (4개) ->140 개.
# for 문으로 140 번 돌려도 같은 답이지만 @가 수백배는 빠릅니다 . 그래서 numpy를 쓴다.


def 예측(Z, w, b):
    return Z @ w + b


def 손실(Z, y, w, b):  # 01 과 완전히 같음: (실제 − 예측)² 의 평균 = MSE
    return np.mean((y - 예측(Z, w, b)) ** 2)


# 기울기 ( 어느 쪽이 내리막) 는 01 처럼 '살짝 밟아보기'. 손잡이가 5개 (w 4개 + b )니까 5 번 밟아 봅니다.
# 편미분 = 손잡이 여러 개 중 ' 하나만 ' 살짝 움직였을 때 손실이 변하는 정도.
# 아래 for 문 한바퀴가 편미분 한번.
# 그레디언트 = 편미분 5개를 한 줄로 모은것(gw 4개 + gb)." 5개 손잡이 각각의 내리막 방향표"


h = 0.0001  # 학습률 설정


def 기울기_밟아보기(Z, y, w, b):
    gw = np.zeros(len(w))  # 문법 - 담을 빈 칸 4개
    for j in range(len(w)):  # j = 0,1,2,3 : j 번째 손잡이만 움직여 본다
        w_plus, w_minus = w.copy(), w.copy()  # 원본 안건드리기!!
        w_plus[j] += h  # j 번째만 살짝 키우고
        w_minus[j] -= h  # j 번째만 살짝 줄여서
        gw[j] = (손실(Z, y, w_plus, b) - 손실(Z, y, w_minus, b)) / (
            2 * h
        )  # (오른쪽 손실 − 왼쪽 손실) ÷ 거리
    gb = (손실(Z, y, w, b + h) - 손실(Z, y, w, b - h)) / (2 * h)  # b 도 한 번
    return gw, gb


def 학습(Z, y, lr=0.1, epochs=500):
    w = np.zeros(Z.shape[1])  # Z.shape[1] = 열 수 = 4. 가중치 4개를 0 에서 출발
    b = 0.0
    for _ in range(epochs):  # 500 바퀴 (에폭 500) , epochs - 학습횟수
        gw, gb = 기울기_밟아보기(Z, y, w, b)
        w = w - lr * gw  # 경사하강 4개가 한꺼번에 '내리막 쪽으로 보폭 ls 만큼'
        b = b - lr * gb
    return w, b


w, b = 학습(Z_train, y_train)  # 학습 용으로만 배웁니다.
print("\n[4] 학습 완료 — 표준화 눈금의 가중치")
for 이름, wi in zip(
    특징이름, w
):  # zip - 두줄을 나란히 짝지어 돌기 :( 공기 온도, w[0] )( 공기 온도, w[1] )...
    print(
        f"    {이름:6s} w = {wi:+.3f}"
    )  # {:+.3f} = 부호 붙여 소수 3자리 (00 미리보기 ①)
print(f"    절편 b = {b:.3f}")

# 절편 310 = 모든 센서가 '평균값 '일 때의 예측 공정온도 . 표준화 했으니 z = 0 이 평균이라 그렇습니다.
# 공기온도 w += 1.79 : 공기 온도가 표준편차 1 개만큼 오르면 공정온도가 1.79 오른다. 나머지는 거의 0


# =====================================================================
# 5. 채점 — 학습용 점수와 시험용 점수를 '나란히'
# =====================================================================


def MSE(y, yhat):  # 손실과 같은 식. 채점용으로 이름만 따로
    return np.mean((y - yhat) ** 2)


def R2(
    y, yhat
):  # 01 9번의 R². 1 에 가까울수록 좋음, 0 = 평균만 말하는 수준(공식 외울필요없음)
    return 1 - np.sum((y - yhat) ** 2) / np.sum((y - y.mean()) ** 2)


tr_pred = 예측(Z_train, w, b)  # 학습용
te_pred = 예측(Z_test, w, b)  # 시험용
print("\n[5] 채점")
print(
    f"    학습용(train)  R² {R2(y_train, tr_pred):.4f}   손실 {MSE(y_train, tr_pred):.3f}"
)
print(
    f"    시험용(test)   R² {R2(y_test, te_pred):.4f}   손실 {MSE(y_test, te_pred):.3f}"
)

# 학습용 0.81 , 시험용 0.78 시험용이 살짝 낮은게 정상입니다 ( 안 본 문제니까 ) 차이가 0.03 -> 건강

print("""
    읽는 법 — 두 점수를 '같이' 봐야 합니다:
      둘이 비슷하고 둘 다 좋음        → 건강. 규칙을 배운 것
      학습용만 좋고 시험용이 뚝 떨어짐  → 과적합. 답안지를 외운 것
      둘 다 나쁨                     → 과소적합. 모델이 너무 단순하거나 센서가 부족
    어림 기준: R² 차이가 0.05 안이면 정상, 0.10 넘으면 의심. (법칙은 아니고 경보선)
""")
print("    시험용 앞 6대: 예측 vs 실제")
for p, t in zip(te_pred[:6], y_test[:6]):
    print(f"      예측 {p:7.2f}   실제 {t:7.2f}   (차이 {t - p:+.2f})")

# 과적함 = 학습 데이터를 ' 외워서 ' 학습 점수는 좋은데 새 데이터엔 틀리는 상태.
#      - 기출 문제만 달달 외운 학생. 모의고사(학습용) 100점 , 수능(시험용) 60점
#      - 과적합이 나면 -> 현장에 넣는 순간 점수가 무너집니다. 학습 점수만 보고 배포하면 사고나는 이유


# =====================================================================
# 5-1. 과적합을 눈으로 — 학습용을 딱 5대만 주면?
# =====================================================================
print("[5-1] 학습용을 5대만 써서 학습하면? (손잡이 5개 = 데이터 5대)")
w5, b5 = 학습(Z_train[:5], y_train[:5], epochs=2000)
print(
    f"    학습용 5대 R² {R2(y_train[:5], 예측(Z_train[:5], w5, b5)):.4f}   ← 만점. 5대를 통째로 외웠다"
)
print(
    f"    시험용 60대 R² {R2(y_test, 예측(Z_test, w5, b5)):.4f}   ← 140대로 배웠을 때(0.78)보다 뚝. 이게 과적합"
)
print("    가중치도 달라짐:", w5.round(2), "← 140대 때는", w.round(2))


# =====================================================================
# 6. 어느 센서가 중요한가 — 표준화 가중치 크기로 읽는다
# =====================================================================
print("\n[6] 영향력 순위 (가중치 절댓값 큰 순)")
for 이름, wi in sorted(zip(특징이름, w), key=lambda t: -abs(t[1])):
    print(
        f"    {이름:6s} {wi:+.3f}   {'높을수록 공정온도 ↑' if wi > 0 else '높을수록 공정온도 ↓'}"
    )  # 한 줄 if (00 미리보기 ⑤)


# =====================================================================
# 6-1. 센서를 하나씩 빼 보면 — 중요도를 눈으로, 그리고 과소적합을 만나는 자리
# =====================================================================
print(
    f"\n[6-1] 센서 하나 빼고 다시 학습 (전부 쓰면 학습용 {R2(y_train, tr_pred):.4f} / 시험용 {R2(y_test, te_pred):.4f})"
)
for 뺄 in range(4):
    남길 = [j for j in range(4) if j != 뺄]
    w_, b_ = 학습(Z_train[:, 남길], y_train)
    r_tr = R2(y_train, 예측(Z_train[:, 남길], w_, b_))
    r_te = R2(y_test, 예측(Z_test[:, 남길], w_, b_))
    print(f"    {특징이름[뺄]:4s} 빼면 → 학습용 {r_tr:.4f} / 시험용 {r_te:.4f}")

# =====================================================================
# 실습 — 데이터를 얼마나 줄이면 외우기 시작하나
# =====================================================================
# [문제] 5-1 에서 5대만 주니 학습용 R2 가 1.0 (완전 암기) 이 나왔습니다.
# 그럼 20대를 주면 어떨까요? 학습용과 시험용 R2 를 둘 다 구해 보세요.
#
#   힌트: 학습(Z_train[:20], y_train[:20], epochs=2000) 으로 배우고,
# R2 와 예측 함수를 그대로 쓰면 됩니다.
